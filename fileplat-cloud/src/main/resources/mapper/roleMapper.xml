<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hua.fileplat.cloud.user.dao.RoleDao">
    <select id="selectRole" resultType="RoleDto">
        select * from u_role
    </select>

    <update id="updateRoleDto" parameterType="RoleDto">
        update u_role set name=#{name} where id = #{id}
    </update>


    <!--
        <cache />
        myBatis 缓存的使用
        select 语句 会查询缓存
        insert,delete,update 操作过后 会更新缓存
        具体参考 http://www.mybatis.org/mybatis-3/zh/sqlmap-xml.html
    -->



    <!--myBatis 自定义缓存
        1.自定义的缓存必须实现 Cache 接口
        2.编写缓存类
        <cache type="com.domain.something.MyCustomCache">
          <property name="cacheFile" value="/tmp/my-custom-cache.tmp"/>
        </cache>
    -->

</mapper>